daari {{ .Chart.AppVersion }} installed.

  Service: {{ include "daari.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

{{- if eq (include "daari.fleetSqliteWarning" .) "true" }}

WARNING: Effective replicas={{ include "daari.fleetReplicas" . }} with postgres.enabled=false.
  batches, files, and the usage ledger default to per-pod SQLite — GETs can 404
  and counters split across replicas. Set postgres.enabled=true (and postgres.url)
  so the chart wires DAARI_BATCHES__BACKEND / DAARI_FILES__BACKEND /
  DAARI_OBSERVABILITY__BACKEND to postgres, or keep replicaCount and
  autoscaling.minReplicas at 1. See docs/developer/guides/features/batches.md.
{{- else if .Values.postgres.enabled }}

Postgres enabled: observability, batches, and files backends are set to postgres.
{{- end }}

Image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
