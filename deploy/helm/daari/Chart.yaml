apiVersion: v2
name: daari
description: Local-first LLM execution router (gateway replicas + optional Redis)
type: application
# Chart packaging version (semver for the chart itself). Bump on chart-only changes.
version: 0.2.0
# Must match pyproject.toml / daari.__version__ / values image.tag after each release.
# Agents/humans: bump appVersion + values.yaml image.tag together; do not create git tags here.
appVersion: "1.4.0"
