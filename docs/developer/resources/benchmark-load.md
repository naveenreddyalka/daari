# Benchmark: load

Generated by `python scripts/bench_load.py` against a hermetic `daari serve`
and real Ollama. Three mixes: a warmed L0 replay (cache), unique
no-cache generations with `max_tokens` capped (generate), and a
tool-bearing agent replay (agent, G1 / #223). Reproduce:
[guide](../guides/observability/live-benchmark.md#load).

- **Date:** 2026-08-26
- **Commit:** `c56999c`
- **Hardware:** Apple M4 Pro, 48 GB RAM
- **Ollama:** 0.18.2

## Mixes

| Mix | Requests | Concurrency | RPS | p50 ms | p95 ms | L0 hit | Errors |
|-----|----------|-------------|-----|--------|--------|--------|--------|
| cache | 200 | 16 | 219.7 | 68 | 89 | 100% | 0 |
| generate | 16 | 2 | 6.0 | 288 | 458 | 0% | 0 |
| agent | 80 | 8 | 217.8 | 36 | 42 | 100% | 0 |

Cache and agent RPS are the replay phase only (warm-up is excluded).
The L0 hit column is `daari_meta.cache_hit` on those replay requests.
Generate requests carry `X-Daari-No-Cache` and a small `max_tokens`
so the run is a latency/throughput sample, not a quality eval.

The agent mix sends the same short prompts with an OpenAI `tools`
schema attached — the Cursor/Claude Code shape. Implied frontier $
is gpt-4o list input rates on prompt tokens of L0 hits (priced, not
billed).

## Agent prefix (G1)

- **Prompt tokens on L0 hits:** 7280
- **Implied frontier $ avoided:** $0.0182 (gpt-4o input, priced not billed)
