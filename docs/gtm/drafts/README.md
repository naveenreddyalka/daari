# GTM drafts

```bash
python scripts/gtm_shipping_note.py
```

Writes `shipping-note.md` from the latest released section in `CHANGELOG.md`, plus short X and LinkedIn variants. A human publishes. Never run this into a third-party API.
